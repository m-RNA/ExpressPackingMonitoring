<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit">

    <ControlTemplate x:Key="DoubleUpDownTemplate" TargetType="{x:Type xctk:DoubleUpDown}">
        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                BorderThickness="{TemplateBinding BorderThickness}" 
                Background="{TemplateBinding Background}" 
                CornerRadius="6" Padding="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="PART_TextBox" 
                         Background="Transparent" 
                         BorderThickness="0" 
                         Foreground="{TemplateBinding Foreground}" 
                         HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" 
                         VerticalContentAlignment="Center" 
                         Padding="{TemplateBinding Padding}"
                         Margin="0" 
                         Focusable="True" />
                
                <xctk:ButtonSpinner x:Name="PART_Spinner" 
                                    IsTabStop="False" 
                                    Background="Transparent" 
                                    Margin="0,0,4,0"
                                    BorderThickness="0" 
                                    Grid.Column="1" 
                                    AllowSpin="{TemplateBinding AllowSpin}" 
                                    ShowButtonSpinner="{TemplateBinding ShowButtonSpinner}">
                </xctk:ButtonSpinner>
            </Grid>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="IntegerUpDownTemplate" TargetType="{x:Type xctk:IntegerUpDown}">
        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                BorderThickness="{TemplateBinding BorderThickness}" 
                Background="{TemplateBinding Background}" 
                CornerRadius="6" Padding="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="PART_TextBox" 
                         Background="Transparent" 
                         BorderThickness="0" 
                         Foreground="{TemplateBinding Foreground}" 
                         HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" 
                         VerticalContentAlignment="Center" 
                         Padding="{TemplateBinding Padding}"
                         Margin="0" 
                         Focusable="True" />
                
                <xctk:ButtonSpinner x:Name="PART_Spinner" 
                                    IsTabStop="False" 
                                    Background="Transparent" 
                                    Margin="0,0,4,0"
                                    BorderThickness="0" 
                                    Grid.Column="1" 
                                    AllowSpin="{TemplateBinding AllowSpin}" 
                                    ShowButtonSpinner="{TemplateBinding ShowButtonSpinner}">
                </xctk:ButtonSpinner>
            </Grid>
        </Border>
    </ControlTemplate>

    <Style TargetType="{x:Type xctk:DoubleUpDown}">
        <Setter Property="Background" Value="{DynamicResource PanelBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Template" Value="{StaticResource DoubleUpDownTemplate}" />
    </Style>

    <Style TargetType="{x:Type xctk:IntegerUpDown}">
        <Setter Property="Background" Value="{DynamicResource PanelBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Template" Value="{StaticResource IntegerUpDownTemplate}" />
    </Style>

</ResourceDictionary>
