<Window x:Class="ExpressPackingMonitoring.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="打包数据深度分析" Height="720" Width="1100"
        WindowStartupLocation="CenterOwner" Background="#F8FAFC"
        Icon="app.ico" FontFamily="Microsoft YaHei UI">

    <Window.Resources>
        <!-- 新增：定义统计窗口需要的按钮样式 -->
        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 新增：维度切换按钮样式 -->
        <Style x:Key="MetricButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentBlue}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 筛选栏 -->
            <RowDefinition Height="Auto"/> <!-- 汇总卡片 -->
            <RowDefinition Height="*"/>    <!-- 图表区 -->
        </Grid.RowDefinitions>

        <!-- 1. 增强型工具栏 -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="20,15" Margin="0,0,0,15" BorderBrush="#E2E8F0" BorderThickness="1">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="时间范围: " VerticalAlignment="Center" FontWeight="Bold"/>
                    <ComboBox x:Name="PresetCombo" Width="100" SelectionChanged="OnFilterChanged" SelectedIndex="1">
                        <ComboBoxItem Content="本周" Tag="Week"/>
                        <ComboBoxItem Content="本月" Tag="Month"/>
                        <ComboBoxItem Content="最近30天" Tag="Last30"/>
                        <ComboBoxItem Content="全部" Tag="All"/>
                    </ComboBox>
                    <DatePicker x:Name="PickerStart" Margin="10,0,5,0" Width="120" SelectedDateChanged="OnFilterChanged"/>
                    <TextBlock Text="-" VerticalAlignment="Center"/>
                    <DatePicker x:Name="PickerEnd" Margin="5,0,0,0" Width="120" SelectedDateChanged="OnFilterChanged"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="查看维度: " VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                    <RadioButton Content="数量" IsChecked="True" x:Name="ModePieces" Style="{StaticResource MetricButtonStyle}" Click="OnFilterChanged"/>
                    <RadioButton Content="时长" x:Name="ModeDuration" Style="{StaticResource MetricButtonStyle}" Click="OnFilterChanged" Margin="5,0"/>
                    <RadioButton Content="大小" x:Name="ModeSize" Style="{StaticResource MetricButtonStyle}" Click="OnFilterChanged"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="聚合方式: " VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                    <ComboBox x:Name="GroupCombo" Width="80" SelectionChanged="OnFilterChanged" SelectedIndex="0">
                        <ComboBoxItem Content="按日" Tag="day"/>
                        <ComboBoxItem Content="按周" Tag="week"/>
                        <ComboBoxItem Content="按月" Tag="month"/>
                    </ComboBox>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- 2. 汇总统计卡片 -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="4" Margin="0,0,0,15">
            <Border Background="White" CornerRadius="8" Margin="0,0,10,0" Padding="15">
                <StackPanel>
                    <TextBlock Text="打包总件数" Foreground="#666" FontSize="12"/>
                    <TextBlock Text="{Binding SummaryPieces}" FontSize="24" FontWeight="Bold" Foreground="#2D3E50"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="8" Margin="0,0,10,0" Padding="15">
                <StackPanel>
                    <TextBlock Text="占用存储空间" Foreground="#666" FontSize="12"/>
                    <TextBlock Text="{Binding SummarySize}" FontSize="24" FontWeight="Bold" Foreground="#3498DB"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="8" Margin="0,0,10,0" Padding="15">
                <StackPanel>
                    <TextBlock Text="累计工作时长" Foreground="#666" FontSize="12"/>
                    <TextBlock Text="{Binding SummaryDuration}" FontSize="24" FontWeight="Bold" Foreground="#27AE60"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="平均单件用时" Foreground="#666" FontSize="12"/>
                    <TextBlock Text="{Binding SummaryAvgTime}" FontSize="24" FontWeight="Bold" Foreground="#E67E22"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- 3. 图表显示区 -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="30,50,30,20">
            <Grid>
                <!-- Y轴背景线与刻度 -->
                <ItemsControl ItemsSource="{Binding YAxisLabels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="60" VerticalAlignment="Bottom">
                                <Border BorderBrush="#F1F5F9" BorderThickness="0,1,0,0"/>
                                <TextBlock Text="{Binding}" Foreground="#94A3B8" FontSize="11" HorizontalAlignment="Right" Margin="0,-8,10,0" Width="50" TextAlignment="Right"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 动态柱状图 -->
                <ItemsControl ItemsSource="{Binding ChartData}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="1,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 柱体：增加触发器实现丝滑变色 -->
                                <Border Grid.Row="0" VerticalAlignment="Bottom" 
                                        Background="#334155" CornerRadius="3,3,0,0"
                                        Height="{Binding BarHeight}" MaxWidth="45">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#3B82F6"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <!-- 关键：多维度 Tooltip -->
                                    <Border.ToolTip>
                                        <ToolTip Background="White" BorderThickness="0" Padding="10">
                                            <ToolTip.Effect>
                                                <DropShadowEffect BlurRadius="10" Opacity="0.1"/>
                                            </ToolTip.Effect>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DateFull}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8" Foreground="#1E293B"/>
                                                <Grid MinWidth="150">
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>

                                                    <TextBlock Grid.Row="0" Text="📦 打包件数" Foreground="#64748B"/>
                                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Pieces, StringFormat={}{0} 件}" FontWeight="Bold" HorizontalAlignment="Right"/>

                                                    <TextBlock Grid.Row="1" Text="⏱ 总计时长" Foreground="#64748B" Margin="0,5"/>
                                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TotalTime}" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,5"/>

                                                    <TextBlock Grid.Row="2" Text="💾 占用空间" Foreground="#64748B"/>
                                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SizeText}" FontWeight="Bold" HorizontalAlignment="Right"/>
                                                </Grid>
                                            </StackPanel>
                                        </ToolTip>
                                    </Border.ToolTip>
                                </Border>

                                <!-- X轴标签 -->
                                <StackPanel Grid.Row="1" Margin="0,10,0,0" Visibility="{Binding LabelVisibility}">
                                    <TextBlock Text="{Binding DateLabel}" HorizontalAlignment="Center" FontSize="10" Foreground="#64748B"/>
                                    <TextBlock Text="{Binding DateSub}" HorizontalAlignment="Center" FontSize="9" Foreground="#94A3B8"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</Window>